{
  "name": "sgz-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "develop:frontend": "pnpm run --dir=frontend dev",
    "develop:backend": "pnpm run --dir=backend start:dev",
    "develop": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "cross-env": "7.0.3"
  },
  "author": ""
}
